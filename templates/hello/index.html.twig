 {% extends 'base.html.twig' %}

  {% block body%}
      {{ parent() }}
      {{ include('fragments/sayhello.html.twig', {
          name: name, firstanme: firstname
      }) }}
      {{ include('fragments/sayhello.html.twig', {
          name: 'Chahd', firstanme: 'Sghaier'
      }) }}

      {{ render(controller('App\\Controller\\FirstController::notes', {nb: 10})) }}
  {% endblock %}

 {% block title %}
     {{ parent() }} / Hello
 {% endblock %}